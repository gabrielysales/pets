<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>{{#if prestador.id}}Editar Prestador{{else}}Novo Prestador{{/if}}</h2>

<form method="POST" action="/prestadores/salvar">
  {{#if prestador.id}}<input type="hidden" name="id" value="{{prestador.id}}">{{/if}}
  
  <label>Nome:</label><br>
  <input name="nome" value="{{prestador.nome}}" required><br><br>
  
  <label>Tipo de Serviço:</label><br>
  <input name="tipo_servico" value="{{prestador.tipo_servico}}" required><br><br>
  
  <label>Email:</label><br>
  <input name="email" value="{{prestador.email}}" required><br><br>
  
  <label>Telefone:</label><br>
  <input name="telefone" value="{{prestador.telefone}}" required><br><br>
  
  <label>Endereço:</label><br>
  <input name="endereco" value="{{prestador.endereco}}" required><br><br>
  
  <button type="submit">Salvar</button>
</form>

<a href="/prestadores">⬅ Voltar</a>

</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>{{#if tutor.id}}Editar Tutor{{else}}Novo Tutor{{/if}}</h2>

<form method="POST" action="/tutores/salvar">
  {{#if tutor.id}}<input type="hidden" name="id" value="{{tutor.id}}">{{/if}}
  <label>Nome:</label><input name="nome" value="{{tutor.nome}}" required><br>
  <label>Email:</label><input name="email" value="{{tutor.email}}" required><br>
  <label>Telefone:</label><input name="telefone" value="{{tutor.telefone}}" required><br>
  <label>Endereço:</label><input name="endereco" value="{{tutor.endereco}}" required><br>
  <button type="submit">Salvar</button>
</form>

<a href="/tutores">⬅ Voltar</a>

</body>
</html>